name: Renovate changelog attribution

# Generated by Verjson/.github scripts/gen-changelog-caller.sh renovate-attribution 413bf03b179ff3028e6c7da5551aaa44562ddd8d
# — do not edit by hand. This pull_request_target caller executes only the
# immutable trusted workflow below and never checks out pull-request code.
on:
  pull_request_target:
    types: [opened, reopened, synchronize]

permissions:
  contents: read
  pull-requests: read

jobs:
  renovate-changelog:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      (github.event.pull_request.user.login == 'app/renovate' ||
       github.event.pull_request.user.login == 'renovate[bot]') &&
      startsWith(github.event.pull_request.head.ref, 'renovate/')
    uses: Verjson/.github/.github/workflows/renovate-changelog.yml@413bf03b179ff3028e6c7da5551aaa44562ddd8d
    with:
      contract_ref: 413bf03b179ff3028e6c7da5551aaa44562ddd8d
      release_app_client_id: ${{ vars.RELEASE_APP_CLIENT_ID }}
    secrets:
      release_app_private_key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
